// Extra product photos by product id (the main photo is the product's `image`).
// Filled in from each product's original gallery; edit per product in Admin → Products.
const galleries = {}

export default galleries
